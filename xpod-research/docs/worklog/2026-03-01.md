# 工作记录（2026-03-01）

**操作人**：leo

**操作日期**：2026年3月1日

**操作环境**：
- 物理机：leosuek8s（Ubuntu，计划部署 k8s v1.31.14）
- 虚拟化：KVM，后续在该物理机上开 8 台 VM 作为 k8s 节点

**操作目标**：明确实验问题与整体架构，确定后续以“单物理机 + 8 台 VM + GPU（A10）”形式搭建统一调度 testbed。

## 论文可用信息（用于后续直接写进论文/附录）
**研究问题/假设**
- 三要素（算法/算力/数据）统一调度相对“仅调度算力”的收益可量化，且可通过可复现实验流程验证。

**实验/实现范围**
- 范围：项目启动；规划并开始部署 Kubernetes 实验环境。
- 不包含：调度算法实现与实验对比数据产出（待后续阶段完成）。

**可复现要素**
- 输入数据：待补充（该阶段以环境部署为主）
- 输出产物：可运行的 k8s 集群（里程碑目标）
- 随机种子：不适用/待补充
- 环境信息：物理机 leosuek8s（72 核 / 251G / 1.8T，规划使用 HAMi vGPU 管理 A10 显卡），计划通过 KVM 创建 8 台 VM 组成 k8s 集群，使用 192.168.100.0/24 网段进行节点间通信。

## 今日记录（按“每次我做了啥”）

### 1) 项目启动与环境部署启动
**目的**
- 明确实验平台的最小可用形态，为后续 Volcano/Fluid/Koordinator 以及调度原型验证提供载体。

**做了什么**
- 明确项目从“研究问题”到“可跑实验”的最短路径：
  - 先搭建可复现的 k8s testbed → 再接入 Volcano/Fluid/Koordinator → 再做离线/回放调度评测与图表产出
- 做实验环境规划（为后续写进论文的 Setup 打底）：
  - 规划节点规模与角色：最终确定为“1 台物理机控制平面 + 8 台服务节点 VM”，服务节点按照 gpu-node/data-node/cpu-node/algo-node 划分角色，对应后续统一调度的三类服务。
  - 规划 VM 资源与 IP：预留 192.168.100.0/24 网段，GPU 节点使用 .11–.13，CPU 节点使用 .21–.22，数据节点使用 .31–.32，算法节点使用 .41，保证命名与 IP 可以直接写入论文附录。
  - 规划命名规则与标签体系：节点名采用 {gpu,data,cpu,algo}-node-X，并在 k8s 中预留 node-role.kubernetes.io/{gpu-compute,data,algorithm,cpu-compute} 等标签，方便后续按服务类型路由。
  - 规划网络与地址分配：统一通过 KVM 虚拟网卡桥接到物理机，内部走 192.168.100.0/24 内网，外部通过物理机统一出口访问互联网，便于统一运维与监控。
- 做部署方案选型与风险清单：
  - 部署方式：选择 “kubeadm + containerd” 的原生方案，k8s 版本锁定为 v1.31.14，以便与 Volcano/Fluid/Koordinator 等组件的官方支持版本保持一致。
  - 后续组件兼容性：标记 Volcano/Fluid/Koordinator 与 k8s 的版本矩阵为风险点，后续在安装阶段通过实际部署与 kubectl get pods/crd 验证兼容性。
- 启动 Kubernetes 集群部署工作（进入实操阶段，具体命令/脚本待补充）。

**结果/产出**
- 项目进入“环境搭建”阶段，形成明确里程碑：在 3/15 前完成 k8s 可用部署。

**对论文的贡献**
- 对应章节：实验设置/系统实现（Testbed / Implementation）
- 计划图表/表格：实验环境配置表（节点数、CPU/GPU、网络）
- 可引用结论（一句话）：统一调度原型需要可控、可复现的多节点环境作为评测基座。

**可复现信息（可选，写关键项即可）**
- 入口脚本/模块：待补充（部署方式/脚本/命令）
- 关键参数：节点数（后续确定为 8 台 VM）
- 产物路径：待补充

**风险/待验证**
- 版本兼容性：k8s 与 Volcano/Fluid/Koordinator 的版本匹配关系需要验证（待后续安装阶段确认）。
